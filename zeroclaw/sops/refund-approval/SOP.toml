[sop]
name = "refund-approval"
description = "Require explicit owner approval before generating an unsigned refund request."
version = "0.1.0"
priority = "high"
execution_mode = "step_by_step"
max_concurrent = 1
admission_policy = "hold"
max_pending_approvals = 8

[[triggers]]
type = "manual"
